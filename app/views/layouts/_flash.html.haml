#flash
  - flash.each do |type, msg|
    %div{ class: "alert alert-#{type == :notice ? "success" : type} fade", "data-dismiss" => "alert" }
      %button.close &times;
      = msg if msg.is_a?(String)
