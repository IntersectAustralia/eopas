#media_item
  %h2 Upload Media Item

  = form_for @media_item, :html => { :multipart => true, :class => 'validate'} do |f|
    = error_messages @media_item

    = render :partial => 'form', :object => f

    = f.submit "Create"

  * required attributes

.info
  #uploading
    %h3 Media Uploading
    %p Please note that transcoding happens in the background and video will not display until after this is complete. Thanks for your patience.

  %h3
    Please enter information about your media resource.
  %p
    Please note that the upload limit of this server is
    %span{:style => "color: red;"}
      1GB
    , but large files will take a long time to upload and transcode.
  %p
    You can find out more about the Licenses at:
  %ul
    - License.all.each do |l|
      %li
        #{l.code} :
        %a{:href => l.url}
          = l.name